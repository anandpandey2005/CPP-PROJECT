// worst case O(n) 
// mostly function works  with O(1) time complexity
// space complexity O(n) because we are storing all elements in the array


#include<bits/stdc++.h>
using namespace std;

int main(){
    unordered_set<int> ust;
    // loewer bound  & upper  bound  functions does'nt work
    // rest functions same as previous one 
    // particular order it has a better complexity than set in most cases,except some collision happen

}